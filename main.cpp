#include <Windows.h>
#include <unistd.h>
#include <iostream>
#include <cstdlib>
using namespace std;

int main()
{
   cout << "Searching for errors in windows.";
   cout << "This may take a while";
   sleep(3600);
   cout << "No errors found."
   return 0;
}
